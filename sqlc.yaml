version: "2"
sql:
  - schema: "db/migrations/"
    queries: "db/queries/"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "app"
        emit_json_tags: true

#  Globbing for Schema and Queries /**/*.sql